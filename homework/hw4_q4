
def list_min(lst,low,high):
    if low>=high:
        return lst[low]
    else:
        curr=list_min(lst,low+1,high)
        if curr<lst[low]:
            return curr
        else:
            return lst[low]
